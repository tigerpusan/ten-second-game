{
  "name": "ten-second-game-app",
  "version": "1.0.0",
  "private": true,
  "description": "10초 맞추기 - simple timing game",
  "scripts": {
    "cap:add:android": "npx cap add android",
    "cap:copy": "npx cap copy android",
    "build:android": "npm run cap:copy && cd android && ./gradlew assembleDebug"
  },
  "dependencies": {
    "@capacitor/android": "^7.2.0",
    "@capacitor/core": "^7.2.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.2.0"
  }
}
